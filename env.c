#include "file.h"
#include <stdio.h>

/**
 * print_env - Function to print environment variables
 *
 * Return: Nothing
 */
void print_env(vars_t *vars)
{
/*	int i;
	int argc;
	char **argv;
	char **envp;
*/	
	(void)vars;
/*
	for (i = 0 ; envp[i] ; i++)
		printf( "envp[%d]: %s\n", i, envp[i] );
*/
}

